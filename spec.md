# 随心写作 — 产品与技术规格说明书（Spec v1.0 + v2 修订）

> 状态：v1 已实现；v2 修订已实现（见下方"v2 修订"）
> 更新日期：2026-09-24
> 目标用户：单用户（作者本人），桌面端本地使用

## v2 修订（优先于正文中的对应条目）

评审（产品设计视角）后做了以下改动，详细理由与验收见 [docs/改进计划.md](./docs/改进计划.md)：

| 条目 | v1 | v2 |
|---|---|---|
| 定位 | 导入 → 拆块 → 改 → 导出的改稿台 | 能从空白开始写，也能改稿 |
| §3.2 粒度 | 全局"句子 / 段落 / 全文"开关，底层按句子存储 | 取消开关；**段落是持久单位**，句子只在显示与选区时临时切分；AI 作用范围由选区决定（一段 / 多段 / 段内片段 / 一节） |
| §3.1 模型 | 独立的大纲树 + 块挂靠 `outlineNodeId` | 标题块就在正文里，大纲由标题生成；每段独立版本历史 |
| §4 F3 | 编辑 E / 提意见 R / 重写 T | 编辑 E；AI 指令框（空格），留空 = 润色，另有快捷指令与续写 |
| §4 F4 | 字符级 diff，逐簇确认，Esc 全部拒绝，裁决完自动写入 | 按词比较、按分句聚簇；标注 / 修改后 / 对照；改动大整体决定；接受 / 改一改 / 再来一版 / 放弃 / 稍后（Esc）；不自动提交；过期检测 |
| §4 F5 | 大纲节点挂靠、对齐检查散落 | 拖动大纲移动整节、升降级；节级扩写 / 精简 / 检查；AI 划分章节先给提案；新增全文检查 |
| §4 F6 | 30s 写入单一恢复槽；Ctrl+S 导出 JSON | 文稿库 + 自动保存（0.8s）；导入永远另起一篇；Ctrl+S 保存、Ctrl+Shift+S 另存为、Ctrl+Shift+E 导出 Markdown |
| §4 F7 | 端点与模型存工程文件 | AI 服务为应用级设置，工程文件里的地址不参与请求；服务商预设；风格代替温度 |
| §5 Schema | `ai-writer/project@1` | `suixin/project@2`（见 README），读取 v1 自动迁移 |
| §6.3 快捷键 | 见下文 | 以应用内帮助（`?`）与 README 为准 |

---

## 1. 产品概述

一款 **块级 AI 协作写作桌面应用**。核心心智模型：

```
导入 → 拆块 → 点选块 → 改（手动/提意见/AI重写）→ 内联 diff 确认 → 导出
```

与传统编辑器 + AI 侧边栏的本质区别：**正文被拆解为可寻址的内容块（Block）**，每个块是独立的编辑与 AI 操作单元，并且与大纲节点双向关联。AI 的每一次产出都必须经过用户在内联 diff 视图中逐处确认后才生效。

### 1.1 设计原则

1. **作者主权**：AI 永不直接改写正文，一切变更经 diff 确认。
2. **手感优先**：所有高频操作有快捷键；动画克制而连贯；阅读排版按长文写作标准打磨。
3. **状态可移植**：工程 JSON 自描述、可存档/读档，且可被外部 agent 解析续作。
4. **本地优先**：单用户、无账号、无云依赖；API Key 仅存本地系统安全区。

---

## 2. 已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 应用形态 | Tauri 桌面应用（Windows 优先），单用户 |
| AI 接入 | 用户自配 OpenAI 兼容 API Key（可接 DeepSeek/通义等），前端直调 |
| AI 结果呈现 | 内联 diff，逐处接受/拒绝 |
| 大纲角色 | 大纲驱动结构：大纲节点 ↔ 正文块双向关联 |
| JSON 导出 | 存档/读档 + 外部 agent 可还原状态、读取未决修改建议 |
| 布局 | 左右双栏：左大纲树，右正文块流 |
| 块边界 | 自动拆分 + 允许手动合并/拆分 |
| 导入格式 | 剪贴板粘贴 + .md / .txt 文件 |

---

## 3. 核心概念与数据模型

### 3.1 领域模型

```
Project
├── meta          标题、创建/修改时间、schema 版本
├── settings      模型端点、模型名、温度等（不含 API Key）
├── outline       OutlineNode 树
│     └── OutlineNode { id, title, level, children[] }
├── blocks        Block[] （正文的有序块序列）
│     └── Block { id, outlineNodeId?, text, status, versions[] }
└── suggestions   Suggestion[] （未决修改建议，含 AI 待确认产出）
```

**Block 状态机**：

```
clean ──选中──> active ──AI处理中──> pending ──diff确认──> clean（接受）
                                    │
                                    └──diff确认──> clean（拒绝，回退原文）
clean ──手动编辑──> dirty ──失焦/保存──> clean
```

**Block.status**：`clean | active | pending | dirty`
- `active`：当前选中块，UI 高亮 + 浮动操作条
- `pending`：AI 已产出 diff，等待用户确认；此状态是外部 agent 最关心的
- `dirty`：手动编辑未落盘

### 3.2 块粒度（Granularity）

三档视图粒度，**随时切换、不丢状态**（块边界在底层按句子存储，段落/全文是聚合视图）：

| 粒度 | 说明 |
|---|---|
| 句子 | 最小操作单元。中文按 `。！？；…` 断句，英文按 `. ! ?`，优先用 `Intl.Segmenter` |
| 段落 | 按换行/空行聚合，写作主战场，默认粒度 |
| 全文 | 整篇一个块，用于整体润色、风格统一 |

手动合并 = 相邻块并为一个；手动拆分 = 光标处一分为二。手动调整产生新的块 id 映射，写入 history。

---

## 4. 功能需求

### F1 导入

- **粘贴导入**：首屏大粘贴区，`Ctrl+V` 即导入；自动识别 Markdown 结构（`#` 标题 → 大纲节点，正文 → 段落块）。
- **文件导入**：拖入或选择 `.md` / `.txt` 文件；txt 按空行分段，无大纲时大纲面板显示"未建立大纲"，可后续手动建。
- **大纲与正文可分离导入**：支持只贴大纲、只贴正文、或一份 md 两者皆有。只贴正文时，可用 AI 反推大纲（M3）。
- 导入后立即自动拆块并展示，整个过程无中间确认页。

### F2 拆块与块浏览

- 顶部粒度切换器（句子 / 段落 / 全文），键盘 `1 / 2 / 3` 切换。
- 块渲染为阅读态卡片流，hover 微抬升 + 左侧出现块锚点，点击进入 active。
- 手动调整：块锚点处右键或拖拽边界 → 合并相邻块 / 在光标处拆分。

### F3 块级操作（核心交互）

块进入 active 后，浮动操作条提供三个动作：

| 动作 | 交互 | 快捷键 |
|---|---|---|
| 编辑 | 块内联变为编辑态，直接改文字，`Esc` 取消，`Ctrl+Enter` 确认 | `E` |
| 提意见 | 块下方展开意见输入框，写自然语言指令（如"这段太啰嗦，压到 50 字内"），AI 按意见产出 diff | `R` |
| 重写 | 不写意见，AI 在保持原意的前提下整块重写，产出 diff | `T` |

- AI 处理时块内显示流式生成（打字机），可随时 `Esc` 中断。
- 生成完成自动切换到内联 diff 视图。

### F4 内联 diff 确认

- 在**原块位置**就地展示 diff：删除内容红色删除线，新增内容绿色高亮；不弹窗、不跳页。
- diff 按"变更簇"分组（相邻改动聚为一处），每处提供 ✓/✗ 逐处接受或拒绝；底部提供"全部接受 / 全部拒绝"。
- 快捷键：`Tab` 在变更簇间跳转，`Y` 接受当前，`N` 拒绝当前，`Enter` 全部接受，`Esc` 全部拒绝。
- 确认结果写入该块的 versions 历史，任意时刻可回退到任一版本。

### F5 大纲驱动结构

- 左栏大纲树与右栏块流**双向定位**：点大纲节点 → 右侧滚动并高亮其下所有块；点块 → 大纲树对应节点展开高亮。
- 块可通过拖拽或右键菜单挂到某个大纲节点下（`outlineNodeId`）。
- 大纲节点级 AI 操作（作用于其下所有块）：
  - **扩写**：节点下内容单薄时，AI 基于大纲意图扩写为正文 diff。
  - **对齐检查**：AI 检查该节点下的块是否偏离大纲意图，产出的是**建议（Suggestion）**而非直接改动——建议列表出现在块旁，用户可采纳转为 AI 修改。
- 大纲树本身可编辑（增删改节点、拖拽调层级），正文块随节点移动。

### F6 导出

- **导出 Markdown**：大纲转为标题层级，块按序拼接，`versions` 中取当前生效版本；另存为 `.md`。
- **导出工程 JSON**：完整状态快照（见 §5），另存为 `.json`。
- **读档**：打开工程 JSON 完全恢复现场，包括未确认的 pending diff、修改意见记录、版本历史。
- 编辑过程中每 30s 自动保存到本地恢复文件（崩溃保护），显式导出仍由用户触发。

### F7 设置

- API 端点（baseURL）、API Key、模型名、温度；Key 经 Tauri keyring 存系统安全存储，**永不写入工程 JSON**。
- 提供"测试连接"按钮验证可用性。
- 未配置 Key 时 AI 按钮置灰并提示去设置，但**不影响**其余全部功能（导入/拆块/手动编辑/导出）。

---

## 5. 工程 JSON Schema（v1）

设计目标：**自描述**——外部 agent 在没有任何先验知识的情况下读到这份 JSON，能理解文章结构、当前进度、以及"有哪些修改建议待处理"。

```jsonc
{
  "schema": "ai-writer/project@1",        // schema 标识与版本，外部据此解析
  "meta": {
    "title": "示例文章",
    "createdAt": "2026-09-24T10:00:00+08:00",
    "updatedAt": "2026-09-24T11:30:00+08:00",
    "language": "zh"
  },
  "settings": {
    "model": "deepseek-chat",
    "baseURL": "https://api.deepseek.com/v1",
    "temperature": 0.7
    // 注意：apiKey 永不出现在此文件
  },
  "outline": [
    {
      "id": "on_1",
      "title": "引言",
      "children": [
        { "id": "on_1_1", "title": "研究背景", "children": [] }
      ]
    }
  ],
  "blocks": [
    {
      "id": "b_1",
      "outlineNodeId": "on_1_1",          // 可为 null（未挂靠）
      "order": 0,
      "text": "当前的生效文本。",
      "status": "pending",                // clean | active | pending | dirty
      "versions": [
        { "v": 0, "text": "原始导入文本。", "source": "import", "at": "..." },
        {
          "v": 1,
          "text": "当前的生效文本。",
          "source": "ai_rewrite",          // import | manual | ai_rewrite | ai_revise | merge | split
          "instruction": null,             // ai_revise 时记录用户意见原文
          "at": "..."
        }
      ]
    }
  ],
  "suggestions": [
    {
      "id": "s_1",
      "blockId": "b_1",
      "kind": "ai_diff",                  // ai_diff（待确认的AI产出）| alignment（大纲对齐建议）
      "instruction": "这段太啰嗦，压到50字内",
      "proposed": "AI 产出的新文本",
      "diff": [ { "op": "del", "text": "..." }, { "op": "keep", "text": "..." }, { "op": "ins", "text": "..." } ],
      "state": "pending",                 // pending | accepted | rejected
      "createdAt": "..."
    }
  ]
}
```

**外部 agent 续作协议**（写进 README 级注释）：
1. 读 `blocks[status=pending]` 与 `suggestions[state=pending]` 获得待办；
2. 外部处理后，将 `suggestions[].state` 置为 `accepted/rejected`，并按结果更新 `blocks[].text`、追加 `versions`；
3. 不认识的字段必须原样保留（forward-compatible）。

---

## 6. UI/UX 设计（重点打磨项）

### 6.1 布局

```
┌────────────────────────────────────────────────┐
│ 顶栏：标题 | 粒度切换(句/段/篇) | 导出 | 设置      │
├──────────┬─────────────────────────────────────┤
│ 大纲树    │        正文块流（居中限宽）            │
│ (240px,  │   hover 浮起 · 点击选中 · 浮动操作条   │
│  可折叠)  │        内联 diff 就地呈现             │
└──────────┴─────────────────────────────────────┘
```

- 正文区**居中限宽**（中文每行约 32–38 字，≈640–720px），两侧留白；大纲栏可拖拽调宽、可一键收起进入专注模式。
- 字体：正文衬线（思源宋体/Noto Serif SC），UI 无衬线；正文字号 16–17px，行高 1.8。
- 浅色纸张质感为默认主题，深色主题随系统。

### 6.2 交互手感清单

- **选中**：点击块任意位置即选中，块左侧出现 2px 强调色指示条 + 浅底色，浮动操作条从块上方淡入（150ms）。
- **流转动画**：阅读态 ↔ 编辑态 ↔ diff 态的切换用高度过渡动画（200ms ease-out），内容不跳动。
- **流式生成**：打字机逐字出现，块右上角显示"生成中… Esc 中断"。
- **diff 配色**：删除 `#fee2e2` 底 + 删除线；新增 `#dcfce7` 底；变更簇之间有呼吸间距，避免花屏。
- **空状态**：首次打开是一张"稿纸"——中央粘贴区 + "拖入 .md/.txt 文件"提示 + 一句示例引导，无任何弹窗阻断。
- **反馈**：所有操作结果用顶部轻提示（toast 2s 自动消失），不用模态框打断。
- **撤销**：全局 `Ctrl+Z` 覆盖手动编辑与 diff 确认。

### 6.3 快捷键总表

| 键 | 功能 |
|---|---|
| `1 / 2 / 3` | 切换句子/段落/全文粒度 |
| `↑ / ↓` | 在块间移动选中 |
| `E / R / T` | 编辑 / 提意见 / 重写（选中块时） |
| `Tab` `Y` `N` | diff 内跳转 / 接受 / 拒绝 |
| `Ctrl+Enter` | 确认编辑或提交意见 |
| `Esc` | 取消当前操作 / 中断生成 |
| `Ctrl+S` | 导出工程 JSON |
| `Ctrl+Z / Shift+Z` | 撤销 / 重做 |

---

## 7. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 壳 | Tauri 2（Rust） | 轻量、本地文件系统与 keyring 能力 |
| 前端 | React 18 + TypeScript + Vite | 精细 UI 生态最成熟 |
| 状态 | Zustand + Immer | 块级细粒度更新、history 快照好做 |
| 块编辑 | 自研阅读态渲染 + 编辑态 textarea/contenteditable | 块粒度场景下比 ProseMirror 简单可控 |
| 断句 | `Intl.Segmenter`（zh/en sentence granularity） | Webview 内置，中日英断句质量高 |
| 文本 diff | diff-match-patch | 字符级 diff，中文按字粒度合适 |
| AI 调用 | fetch 直连 OpenAI 兼容端点，SSE 流式 | 无需后端 |
| 密钥 | tauri-plugin-stronghold 或 keyring | API Key 不落明文文件 |

**Prompt 设计要点**（M2 实现时细化）：
- 重写/意见修改均要求模型**只输出修改后的块文本**，diff 在前端计算（保证 diff 与所见一致）。
- 请求上下文 = 当前块文本 + 所属大纲节点路径 + 相邻前后块各一段（保证连贯）+ 全文大纲摘要。

---

## 8. 非功能需求

- **性能**：5 万字文档拆块渲染首屏 < 1s；块操作反馈 < 100ms；diff 计算 < 200ms。
- **离线**：除 AI 调用外全部功能离线可用。
- **隐私**：文章数据不出本机（仅 AI 请求时发送必要块文本至用户自配端点）。
- **兼容**：Windows 10+ 优先；macOS 不阻塞但不保首期质量。

---

## 9. 边界与异常

| 场景 | 处理 |
|---|---|
| AI 请求失败/超时 | 块恢复原状，toast 报错，可重试；不产生脏状态 |
| AI 返回空或原文照搬 | diff 为空时提示"AI 未作改动"，不进入确认流程 |
| 导入超大文档（>20 万字） | 提示并截断到 20 万字，保留原文备份 |
| 粘贴内容无段落结构 | 按句子粒度兜底拆分 |
| 工程 JSON 损坏/版本不符 | 读档时 schema 校验，不兼容则提示并保留原文件 |
| 编辑中窗口关闭 | 自动保存恢复文件，下次启动提示恢复 |

---

## 10. 里程碑

| 里程碑 | 范围 | 验收 |
|---|---|---|
| **M1 骨架** | Tauri 壳、导入（粘贴/md/txt）、自动拆块、三档粒度、块选中、手动编辑、md/json 导出、自动保存 | 无 AI 也能完成"导入→改→导出"闭环 |
| **M2 AI 核心** | 设置页 + Key 存储、提意见/重写、SSE 流式、内联 diff 逐处确认、版本历史与回退、手动合并/拆分层级块 | 单块 AI 修改全流程手感顺滑 |
| **M3 大纲联动** | 大纲树编辑、块↔节点双向定位与挂靠、节点级扩写/对齐检查、AI 反推大纲 | 大纲驱动写作闭环 |
| **M4 打磨** | 动效与快捷键完善、深色主题、空状态与引导、异常路径补齐 | UI/UX 达到"非常舒服"标准 |

---

## 11. 开放问题（实现前需拍板）

1. **全文粒度的 AI 操作**是否首版开放？5 万字全文重写 token 成本与耗时可观，建议 M2 先限制全文粒度仅支持"提意见"（AI 返回建议列表而非重写全文），M4 再议。
2. **多文档管理**：首版单文档单窗口，启动即恢复上次工程——是否够用？（建议：够用，M4 后再考虑文档列表。）
3. **对齐检查的建议呈现**：并入 suggestions 面板（统一待办）还是散落在块旁（情境更强）？spec 当前取后者，可在 M3 原型阶段 A/B 验证。
